 <style>
.drop-hover:hover > .dropdown-menu {
    display: block !important;
}

</style>

<nav class="navbar navbar-expand-sm navbar-dark bg-dark mb-3 ">
     <%= link_to "CtFinder", root_url, class: "navbar-brand", data: { "turbolinks" => false } %>
     <!--
     <button class="navbar-toggler" type="button"  data-target="#navbarNav4" aria-controls="navbarNav4" aria-expanded="true" >
        <span class="navbar-toggler-icon"></span>
      </button>
      -->
      <div class=" navbar-collapse justify-content-end " id="navbarNav4">
          <ul class="navbar-nav">
              <li class="nav-item ">
                <div class="dropdown drop-hover">
                   <%= image_tag '/prof.png' ,width: '40px', height: '40px' %>
                   <ul class="dropdown-menu dropdown-menu-right">
                   <% if logged_in? %>
                    <li>
                        <%= link_to "My page", user_path(current_user.id), data: { "turbolinks" => false }, class: "dropdown-item" %>
                    </li>
                    <li>
                        <%= link_to "Sign out", logout_url, data: { confirm: "ログアウトします。よろしいですか？" }, class: "dropdown-item", method: "delete" %>
                    </li>
                    </ul>
                    <% else %>   
                  
                    <li>             
                        <a class="dropdown-item" href="/login">Sign in</a>
                    </li>
                    <li >
                        <a class="dropdown-item" href="/users/new">Sign up</a>
                    </li>
                   
                     </ul>
                    <% end %>  
                 </div>

             
         </ul>
     </div>
</nav>